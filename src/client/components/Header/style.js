import styled from "styled-components"

const HeaderStyle = styled.div`
  color: #fff;

  .header-number {
    font-size: 50px;
    font-weight: 400;
    margin: 12px 0 24px;
  }
`

export default HeaderStyle
